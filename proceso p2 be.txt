proceso p4
- Actualizar la configuración de seguridad para permitir el acceso a las rutas de la API (/api/clientes y /api/alquileres para mostrar listado de clientes y de alquileres en formato JSON).
- Actualizar el pom.xml para añadir las dependencias necesarias para trabajar con JSON y Swagger y reload de Maven.
- Crear controladores para la api en cada entidad para pedir datos y devolver JSON.
- Modificar las entidades para evitar que Spring cometa errores con referencias circulares al convertir los datos.
- Crear una clase para configuración de Swagger.
- Comprobar funcionamiento en localhost y resolver errores con las rutas y conflicto de Spring con más de un mapeo. Eliminar los controladores de la api separados por entidad y unificarlo en un solo controlador para la api con ambas entidades. En seguridad añadir rutas para Swagger que no las tenía. Añadir también anotaciones a las entidades para resolver problema de serialización json.
- Peticiones http en Postman, método get a las rutas del localhost.

- Seguridad con tokens con JWT.
- Añdir las dependencias al pom.xml y reload Maven.
- Crear un nuevo paquete de seguridad y añadir una clase para manejar los tokens y otra para el filtro.
- Actualizar la configuración de seguridad para añadir el filtro JWT a la autenticación.
- Crear el modelo de los DTO para requests y autorizaciones y después el controlador de la autenticación.
- Crear controlador para los endpoints seguros y crear los 5.
- Modificarla configuración de Swagger para JWT.
- Prueba para generar token con petició http en Postman, método POST a la ruta del localhost configurando Headers con content-type application/json y el body con json para user admin.
- Prueba para validar token con petición http en Postman, método GET a la ruta del localhost con Auth Bearer Token y el token obtenido en la primera prueba.
- Prueba de uso de token para acceder a endpoint seguro con petición http en Postman, método GET a la ruta del localhost con Auth Bearer Token y el token obtenido en la primera prueba.
